apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: onepassword-secretstore
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    path: onepassword-secretstore/onepassword-secretstore
    repoURL: https://github.com/TwoDCube-Home/argocd
  destination:
    name: in-cluster
    namespace: dev
  syncPolicy:
    automated: {}
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
